# Configuração de deploy no Netlify.
# O Netlify detecta o Next.js e usa o runtime oficial automaticamente.

[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
